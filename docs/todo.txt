TODO

- layers and frames!  8x8 grid of 3 layers w/ 4 frames, all random
- create a test file manually and try to load it!
json.load(file)

- art: drop intermediary lists, generate empty arrays w/ np.empty(shape=(128,1)) and populate them
- palette: store colors in openGL ready format

- investigate whether storing color (vec4) per vertex is better than color index (U of 1D color texture?)

tool research:
http://fontstruct.com/gallery
